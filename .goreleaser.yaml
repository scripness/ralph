version: 2

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}
    binary: scrip

archives:
  - formats: [binary]
    name_template: "scrip-{{ .Os }}-{{ .Arch }}"

release:
  github:
    owner: scripness
    name: ralph

changelog:
  use: github-native
